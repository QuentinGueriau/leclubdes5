package projet

class Sondage {
	//Date dateDebut;
	//Date dateFin;
	int duree;
	static belongsTo = [enseignement: Enseignement];
	static hasMany = [reponses: Reponse]
    static constraints = {
    }
}
